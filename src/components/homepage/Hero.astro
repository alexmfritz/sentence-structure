---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import ColumnTagPill from '../post/ColumnTagPill.astro';
import MenuWheel from '../chrome/MenuWheel.astro';
import HamburgerMenu from '../chrome/HamburgerMenu.astro';
import landingHero from '../../assets/landing-hero.png';

interface Props {
  post: CollectionEntry<'memoir'>;
}

const { post } = Astro.props;
const href = `/memoir/${post.id}`;
---

<section class="hero" data-column="memoir" aria-labelledby="hero-title">
  <Image
    src={landingHero}
    alt=""
    class="hero__image"
    widths={[768, 1280, 1920, 2752]}
    sizes="100vw"
    formats={['avif', 'webp']}
    loading="eager"
    aria-hidden="true"
  />

  <MenuWheel />
  <HamburgerMenu />

  <a class="hero__glass" href={href}>
    <ColumnTagPill column="memoir" />
    <h2 class="hero__title" id="hero-title">{post.data.title}</h2>
    {post.data.deck && <p class="hero__deck">{post.data.deck}</p>}
    <span class="hero__cta" aria-hidden="true">Read piece →</span>
  </a>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: var(--color-bg);
  }

  .hero__image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .hero__glass {
    position: absolute;
    bottom: 2.5rem;
    left: 2rem;
    right: 2rem;
    max-width: 520px;
    padding: 1.75rem 1.75rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
    background: var(--glass-floating);
    backdrop-filter: var(--glass-blur-strong);
    border: 1px solid var(--glass-border);
    border-radius: 4px;
    color: var(--color-text);
    text-decoration: none;
    transition: background 0.2s ease, transform 0.2s ease;
  }

  .hero__glass:hover {
    background: var(--glass-floating-hover);
    transform: translateY(-2px);
  }

  .hero__glass:focus-visible {
    outline: 2px solid var(--color-structural-300);
    outline-offset: 2px;
  }

  .hero__title {
    font-family: var(--font-serif);
    font-size: clamp(36px, 2.3vw + 29px, 52px);
    line-height: 1.1;
    font-weight: 600;
    color: var(--color-text);
    margin: 0;
  }

  .hero__deck {
    font-family: var(--font-serif);
    font-size: 18px;
    line-height: 1.45;
    font-style: italic;
    color: var(--color-text);
    margin: 0;
    max-width: 42ch;
  }

  .hero__cta {
    font-family: var(--font-sans);
    font-size: 14px;
    color: var(--column-200);
    margin-top: 0.25rem;
  }

  @media (max-width: 768px) {
    .hero__glass {
      bottom: 1.25rem;
      left: 1rem;
      right: 1rem;
      padding: 1.25rem;
      gap: 0.6rem;
    }
    .hero {
      aspect-ratio: 4 / 5;
    }
  }

  /* prefers-reduced-transparency handled globally in global.css. */
</style>
